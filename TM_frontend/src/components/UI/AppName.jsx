import React from "react";
import styles from "./AppName.module.css";

function AppName() {
  return (
    <div>
      <h1 className={`${styles["app-head"]}`}>Task Management System</h1>
    </div>
  );
}

export default AppName;
